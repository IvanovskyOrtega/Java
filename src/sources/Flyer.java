package sources;
public interface Flyer {
    void takeOff();
    void land();
    void fly();
}
/*
Agregación, extends, immplements, composición
implements (heredar campos (métodos sin código))
extends (heredar campos, métodos (código))

En la interfaz, los métodos sin código, les llamaremos métodos abstractos, que son semejante a lo que son
los prototipos en C. Es decir, que en la interfaz, solo habrá declaración de métodos.


*/
